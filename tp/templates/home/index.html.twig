<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>TP Symphony !</title>
    <title>{% block title %}Welcome!{% endblock %}</title>
    {% block stylesheets %}{% endblock %}
</head>
<body>
    <h1>Formulaire</h1>
    <form method="get">
        <label for="name">Ville</label>
        <input name="name" id="name" placeholder="Ville" />
        <br>
        <label for="zipcode">Code postal</label>
        <input name="zipcode" id="zipcode" placeholder="Code postal" />
        <br>
        <button type="submit">Chercher</button>
    </form>
    <p>{{ error }}</p>
    {% for city in citys %}
        <ul>
            <li>{{ city['nom'] }}</li>
            <p>Code Postale: {{ city['codesPostaux'][0] }}</p>
            <p>Code Departementale : {{ city['codeDepartement'] }}</p>
            <p>Code Regionale : {{ city['codeRegion'] }}</p>
            <p>Population: {{ city['population'] }}</p>
            <br>
            {#<p>Etablissement:</p>
            {% for etablissement in city['etablissement']['features'] %}
                {% if etablissement != [] %}
                    <ul>
                        <hr>
                        <li>{{ etablissement["properties"]["nom"] }}</li>
                        {% if etablissement["properties"]["adresses"] is defined %}
                            <li>adresse: {{ etablissement["properties"]["adresses"][0]["lignes"][0] }},{{ etablissement["properties"]["adresses"][0]["codePostal"] }} {{ etablissement["properties"]["adresses"][0]["commune"] }}</li>
                        {% endif %}
                        {% if etablissement["properties"]["telephone"] is defined %}
                            <li>telephone: {{ etablissement["properties"]["telephone"] }}</li>
                        {% endif %}
                        {% if etablissement["properties"]["email"] is defined %}
                            <li>Mail: {{ etablissement["properties"]["email"] }}</li>
                        {% endif %}
                        {% if etablissement["properties"]["url"] is defined %}
                            <li>Site: <a href="{{ etablissement["properties"]["url"] }}">{{ etablissement["properties"]["url"] }}</a></li>
                        {% endif %}
                    </ul>
                {% else %}
                    <p>Aucun etablissement trouvé.</p>
                {%  endif %}
            {% endfor %} #}
        </ul>
    {% endfor %}
</body>
</html>
